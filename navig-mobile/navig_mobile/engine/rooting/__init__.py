"""Rooting / jailbreak assist — detect state, guide, and drive user-provided
tools (fastboot / pymobiledevice3). Never one-click: destructive operations are
double-gated (recorded consent + an explicit --yes) at the command layer."""
