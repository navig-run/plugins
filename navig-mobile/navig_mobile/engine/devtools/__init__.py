"""Developer tooling — dynamic instrumentation (frida/objection) and iOS
diagnostics (syslog/crash/pcap). External tools are detected + invoked as
subprocesses, never imported/bundled (frida is heavy/native; objection is GPLv3)."""
