"""Gateway REST routes for navig-mobile — the backend the desktop-OS (and deck)
surfaces render against. Served under ``/api/deck/mobile/*`` and gated on the free
``mobile`` module being enabled. The visual view lives in ``apps/os``; this is the
data/control contract it consumes."""
