"""CLI surface for navig-dedupe. `dedupe_app` is mounted as `navig dedupe` (via the
`navig.commands` entry point) and is also invoked by the standalone `navig-dedupe`/`ndup`
console scripts — one Typer app, two front doors, zero navig imports."""
