"""navig-pipeline — the content assembly line.

Composes the media plugin family (navig-download / -audio / -text / -social) into
one flow via core seams. Each stage is optional: a missing plugin drops its step.
"""

__version__ = "0.1.0"
